# BeoSound 5c Configuration
# Copy to /etc/beosound5c/config.env and customize for each deployment

# Location identifier (sent to Home Assistant webhooks)
DEVICE_NAME="Church"

# BeoRemote One Bluetooth MAC address
# Find with: bluetoothctl → scan on → look for "BeoRemote One"
BEOREMOTE_MAC="48:D0:CF:BD:CE:35"

# Sonos speaker IP address
# Find in Sonos app: Settings → System → About My System
SONOS_IP="192.168.0.190"

# Spotify user ID for playlist fetching
SPOTIFY_USER_ID="mkirsten"
