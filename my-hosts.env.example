# Optional: copy to my-hosts.env to override the default deploy target (beosound5c.local)
#
# Single device:
# BEOSOUND5C_HOSTS="beosound5c.local"
#
# Multiple devices (space-separated):
# BEOSOUND5C_HOSTS="beosound5c-living.local beosound5c-kitchen.local"
